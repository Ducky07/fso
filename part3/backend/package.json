{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Phonebook Nodejs Backend",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf public && cd ../frontend && npm run build",
    "build": "npm install --omit=dev",
    "deploy": "npm run build:ui && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jack Valley",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "mongodb": "^6.16.0",
    "mongoose": "^8.9.5",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
